<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/zw_gray_eee"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:id="@+id/order_btm_con"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal|bottom"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true">

        <ListView
            android:id="@+id/order_pay_types"
            android:layout_width="match_parent"
            android:layout_height="183dp"
            android:cacheColorHint="#00000000"
            android:divider="@null"
            android:scrollbars="none"
            android:dividerHeight="1dp"/>

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:padding="@dimen/activity_horizontal_margin">

             <TextView
                 android:id="@+id/order_pay_for_btn"
                 android:layout_width="match_parent"
                 android:layout_height="50dp"
                 android:layout_alignParentTop="true"
                 android:gravity="center"
                 android:background="@drawable/login_btn_bg_shape"
                 android:textColor="@color/zw_white"
                 android:textSize="16sp"
                 android:text="@string/order_confirm_to_pay"/>

        </RelativeLayout>

    </LinearLayout>

    <ListView
        android:id="@+id/order_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:cacheColorHint="#00000000"
        android:layout_alignParentTop="true"
        android:layout_above="@id/order_btm_con"
        android:divider="@null"
        android:scrollbars="none"
        android:layout_marginBottom="@dimen/activity_horizontal_margin"
        android:dividerHeight="@null"/>

</RelativeLayout>